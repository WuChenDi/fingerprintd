# 设计文档:分量加权模糊匹配引擎

> 归属:fingerprintd PRD §4.3 的展开 ｜ 状态:草案 v0.1 ｜ 2026-07-06
> 目标指标(来自 PRD §3):稳定率 ≥ 95%、碰撞率 ≤ 1%、判定 P99 ≤ 50ms、单实例 ≥ 2k RPS。

---

## 1. 问题定义

输入:一次观测的稳定分量集合 `probe = {c_1, ..., c_n}`。
输出:命中的 `visitorId`(判为同一设备)或判定为新设备,并给出 `confidence`。

三个约束互相拉扯:
- **去雪崩**:单个分量变化(浏览器自动升级、装了新字体、外接显示器)不能生成新 visitorId。
- **防碰撞**:同型号 + 同浏览器版本的两台不同设备不能判为同一 visitorId。
- **高吞吐**:百万级指纹库,不能每请求全库线性扫。

本文的两个核心决策:
1. **两阶段**:候选集生成(§4)→ 概率打分(§5)。
2. **打分用 Fellegi-Sunter(FS)概率记录链接模型**(§5),而非手调加权平均——
   FS 用 `m_i / u_i` 两个参数分别刻画"稳定性"和"区分度",天然对齐上面两个失败模式。

---

## 2. 分量分类(建模前提)

每个分量按两个正交维度刻画,这是所有权重的来源:

- **稳定性**:同设备跨访问,该分量保持不变的概率(→ FS 的 `m_i`)。
- **区分度**:两台不同设备该分量恰好相同的概率(→ FS 的 `u_i`,值越罕见 u 越低)。

| 分量                    | 类型     | 稳定性 | 区分度 | 备注 |
| ----------------------- | -------- | ------ | ------ | ---- |
| WebGL vendor+renderer   | 类别     | 高     | 中     | GPU 硬件,极少变 |
| Canvas hash             | 类别     | 中高   | 高     | 驱动/浏览器升级会整体翻转 |
| Audio fingerprint       | 类别     | 高     | 中高   | 音频栈,稳定 |
| 字体列表                | 集合     | 中     | 高     | 增量变化,适合 Jaccard/MinHash |
| 时区 / 语言 / 平台      | 类别     | 高     | 低     | 熵低,单独不足以识别 |
| 屏幕分辨率+色深         | 数值     | 中     | 中低   | 外接屏会变 |
| CPU 核数 / 设备内存     | 数值     | 高     | 低     | |
| 浏览器版本 / UA         | 版本     | **低** | 中     | **雪崩主源**,每几周自动升级 |
| 插件 / mimeTypes        | 集合     | 中     | 中     | |

> 关键洞察:UA/浏览器版本高频变化,若按精确哈希会不断制造"新设备"——这正是
> FingerprintJS 雪崩的根因。FS 模型下它 `m_i` 低,不匹配时几乎不扣分,自动淡化。

---

## 3. 存储表示(兼顾匹配与合规)

为满足 PRD §7 数据最小化,尽量不存原始值,同时保留匹配能力:

- **类别分量**:存加盐哈希 `H(salt || value)`。仍支持等值比较 + 频率统计(算 `u_i`)。
- **集合分量(字体等)**:存**逐元素哈希**的集合 `{H(f) for f in fonts}`。
  逐元素哈希**保持 Jaccard**,可继续做集合相似度 + MinHash。
- **数值分量**:存原值或分桶值。
- 每 visitorId 额外存:各分量最近值、`first_seen`、`last_seen`、`observation_count`、
  以及漂移所需的按分量新鲜度(§7)。

---

## 4. 阶段一:候选集生成(Blocking)

目标:从百万级库里,用近 O(1) 拿到几十~几百个候选,且**不漏掉真匹配**(召回优先)。

**不能用单一精确 key**——key 里任一分量变了就漏。用**多个独立 blocking key 取并集**做召回冗余:

- `K1 = H(webgl_renderer || platform || timezone)`   —— 全高稳定分量
- `K2 = H(audio_hash || cpu_cores || device_memory)`  —— 另一组高稳定分量
- `K3 = MinHash-LSH(字体集合)` 的 band 桶                —— 容忍字体增量变化
- (可选)`K4 = Simhash(全分量 token)` 的 Hamming 邻桶

召回 = 1 − P(所有 key 都落空)。每个 key 用不相交的稳定分量子集,使"某一分量恰好变了"
时仍有别的 key 命中。候选集 = ∪(各 key 命中的 visitorId)。

**索引结构**:`blocking_key → set<visitorId>` 倒排索引(内存 / Redis Set / PG GIN)。
MinHash-LSH:`band 签名 → 桶 → visitorId`。

**热门配置块膨胀**:iPhone 默认 Safari 这类配置会让某 block 巨大(信息量低)。对策:
- block 大小设上限;超限时该 key **信息量低**,不足以缩小候选,需靠 §5 打分区分;
- **超限必须 `log` 记录丢弃量**(PRD 原则:禁止静默截断),避免"看着覆盖了其实没覆盖"。

---

## 5. 阶段二:概率打分(Fellegi-Sunter)

对每个候选 `cand` 与 `probe` 逐分量比较,累加对数似然比。

### 5.1 单分量一致性 `agree_i`
- 类别:哈希相等 → agree,否则 disagree。
- 集合:Jaccard `J = |A∩B|/|A∪B|`,`J ≥ τ` 记 agree(τ 如 0.8),否则按 J 线性插值(见 5.3)。
- 数值:相等或同桶 → agree。
- 版本:同 major → agree;相邻 major → 部分(见 5.3);否则 disagree。

### 5.2 两个参数
- `m_i = P(agree_i | 同设备)` —— 从**高置信匹配的复访样本**估计,刻画稳定性。
- `u_i = P(agree_i | 不同设备)` —— 从**指纹库该分量值的频率**估计,刻画区分度。
  罕见值 → `u_i` 极低 → 一旦相等即强证据;常见 Chrome/Win 值 → `u_i` 高 → 相等几乎不算数。

### 5.3 分量得分
```
agree    时:  w_i = log2( m_i / u_i )                 // 正分,越罕见越大
disagree 时:  w_i = log2( (1 - m_i) / (1 - u_i) )      // 负分,越稳定越负
部分匹配(集合/版本): w_i = J · log2(m_i/u_i) + (1-J) · log2((1-m_i)/(1-u_i))
缺失(任一方无此分量): w_i = 0                          // 不比较,不计分(见 §8)
```
总分:`score(cand) = Σ_i w_i`。

**为什么这个模型对**:
- **去雪崩**:UA `m_i` 低 → `(1-m_i)/(1-u_i)` 接近 1、log 接近 0 → UA 不匹配几乎不扣分。
- **防碰撞**:两台同型号设备在高熵分量(canvas/字体)上会 disagree,这些分量 `m_i` 高 →
  disagree 的负分很大 → 总分被拉下阈值,判为不同设备。
- **低熵分量自动淡化**:时区/语言 `u_i` 高 → agree 也几乎不加分,不会靠"都在东八区"误判同一。

### 5.4 判定
取最高分候选 `best`,设双阈值 `T_hi > T_lo`:
- `score(best) ≥ T_hi` → **同设备**,返回其 visitorId,执行漂移更新(§7)。
- `T_lo ≤ score(best) < T_hi` → **疑似**,返回该 visitorId 但 confidence 降低并打 `review` 标记。
- `score(best) < T_lo` → **新设备**,签发新 visitorId。
- **有 ≥2 个候选 ≥ T_hi 且分差很小** → 碰撞风险,取最高分并打 `collision_risk` 标记告警。

---

## 6. confidence 输出

`confidence ∈ [0,1]`,由三部分融合(规则加权,首版不上学习模型):
- **匹配裕度**:`score(best)` 超过 `T_hi` 的幅度 + 与第二名的分差(gap 大 → 更确信)。
- **被动信号一致性**:JA4/UA 一致 → 加成;不一致 → 大幅下调(反伪造核心,见 PRD §4.2)。
- **分量完整度**:参与比较的分量数;缺失越多 confidence 越低(§8)。

---

## 7. 漂移更新(模板自适应)与投毒防护

不更新则库里 UA 等分量会过期,几次浏览器升级后匹配退化。规则:
- **仅在高置信匹配(≥ T_hi)时**,用本次观测刷新该 visitorId 的分量最近值与 `last_seen`。
- **防模板投毒**:攻击者可能靠多次低置信匹配把 A 的指纹慢慢改成 B。故:
  - 低置信 / review 命中**不触发更新**;
  - 保留原始 observation 历史,更新只覆盖"最近值"层,不丢历史;
  - 单次更新对分量的变更幅度设界,异常突变打标告警。

---

## 8. 边界情况

- **隐私浏览器屏蔽 canvas(返回 null/空)**:缺失分量记 `w_i = 0` 不参与比较,**绝不能把
  "null canvas" 当一个可匹配值**——否则所有隐私用户在该分量上互相 agree → 大规模碰撞。
  缺失多 → confidence 低。
- **Brave 等每会话随机化 canvas**:同设备该分量每次都变 → `m_i` 趋 0,FS 下自动被忽略,
  回退到字体/audio/webgl 等其他分量。进阶:检测某人群 canvas 恒变 → 对该人群屏蔽该分量(future)。
- **企业统一镜像 / 相同 VM**:真不同设备但指纹全同 → 模糊匹配无解,必然碰撞。
  须靠 IP + 账号行为兜底;文档明确此为**已知能力边界**,不在本引擎解决。

---

## 9. 参数估计与冷启动

- `u_i`:维护每分量值的频率计数,DB 增量更新即可。
- `m_i`:需要"同设备复访"标注。冷启动用**先验**(按 §2 分类:高稳定=0.95、中=0.8、
  低=0.5),上线后用高置信匹配的复访样本做 **EM 迭代**收敛。此为迭代过程,非一次到位。
- 冷库阶段:全判新设备,blocking 索引与频率统计逐步积累。

---

## 10. 离线评估(达标前置)

要证明 §3 指标,须先搭评估集:
- **地面真值**:用登录态 / 长期 cookie 标注"同一设备的多次访问"。
- **稳定率**:同设备跨访问判为同 visitorId 的比例(扫 `T_hi`)。
- **碰撞率**:不同设备判同 visitorId 的比例。
- 网格搜索 `T_lo / T_hi / τ` 与分量先验,画 稳定率-碰撞率 权衡曲线定阈值。
- 上线后持续回灌 review/collision_risk 样本人工复核,反哺 `m_i/u_i` 与阈值。

---

## 11. 数据结构与性能

| 用途          | 结构                                   | 说明 |
| ------------- | -------------------------------------- | ---- |
| Blocking 倒排 | `key → set<visitorId>`                 | 内存 / Redis Set / PG GIN |
| MinHash-LSH   | `band 签名 → 桶`                       | 字体等集合分量召回 |
| 指纹库        | `visitorId → 分量哈希 + 频率物料 + 时间戳` | KV / PG |
| 频率统计      | `分量值哈希 → count`                   | 估 `u_i` |

性能:阶段一 O(候选数),阶段二对候选逐个算 Σ(常数分量数)。候选控制在几百内即可满足
P99 ≤ 50ms。Rust 侧:倒排与打分放内存,频率/指纹持久层异步刷。

---

## 12. 待解决问题

1. 集合分量的 MinHash band 数 / τ 取值,需评估集实测。
2. `m_i` 的 EM 收敛需要多少标注复访样本才稳定?
3. block 大小上限设多少?超限人群的兜底策略(是否强制要求高熵分量 agree)。
4. 频率统计的时间衰减窗口(老旧值频率是否降权)。
5. 打分是否需按"设备人群"分层估参(移动 vs 桌面 m/u 差异大)。
